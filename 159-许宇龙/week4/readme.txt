回溯方法：
本质上这题是穷搜，为了方便，用回溯方法遍历字符串中的每一个间隔。
1. 确定结束条件
若遍历到字符串的最后一个间隔，则在存放结果的集合 res 中加入符号条件的结果 tmp
2. 单层搜索过程
为了避免重复的结果，我们需要确定起始搜索索引 idx。遍历idx到字符串结尾组成的每一个word，若在Dict中，则添加进tmp，并进行下一次回溯
3. 递归返回值和参数
res：符合条件的结果集合
tmp：符合条件的结果
